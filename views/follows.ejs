<!doctype html>
<html lang="en" ng-app="app">
<head>

    <% include includes/meta.ejs %>
    <% include includes/analytics.ejs %>
    <% include includes/dependencies.ejs %>

    <script>

        <% include includes/constants.ejs %>

        app.constant('SUBJECT', {
            kind: data.kind,
            _id: data._id,
            name: data.name,
            handle: data.handle,
            picture: data.picture,
            counts: data.counts
        });

        app.constant('FOLLOWS', data.follows);

    </script>

</head>
<body>

<% include includes/sidebar.ejs %>
<% include includes/logo.ejs %>

<div id="frame">
    <searchbox></searchbox>

    <header></header>
 
    <div class="list" ng-controller="ListController" >
        <item ng-repeat="follow in follows" content="follow" ></item>
    </div>

</div>

</body>

</html>